import os

# constants are used through out the project to ensure using global variables to accomplish certain purposes.
MAX_X = 80
MAX_Y = 20
FRAME_LENGTH = 0.1
PATH = os.path.dirname(os.path.abspath(__file__))
BRICKS = 280
